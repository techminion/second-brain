# Task Queue — Live Operational View

> The canonical backlog is [docs/12_TASKS.md](../docs/12_TASKS.md) (309 tasks, immutable IDs, dependencies, complexity). This file never redefines a task (GOV-2) — it adds the operational state: what is queued *now*, who owns it, and where it stands. Acceptance criteria live with the task's referenced FR/doc section in the backlog.

## Rules

- **Statuses:** `Queued` → `Claimed (<agent>)` → `In Review` → `Done`, plus `Blocked (<reason>)`.
- **Claiming:** set status to `Claimed` with your agent name before writing code; only claim tasks whose dependencies are `Done`; one agent per area prefix at a time (GOV-5).
- **Atomicity:** `S` tasks are 1–4 hour units — claim as-is. `M` tasks: claim whole, but post a sub-checklist in the PR. `L` tasks: **split into sub-units at claim time** and record the split in your AI_HANDOFF entry.
- **Priorities:** `P0` — on the critical path of the current sprint; `P1` — this sprint, parallelizable; `P2` — next in line, claimable if idle.
- When the sprint's queue empties, the architect role promotes the next dependency-ready wave from [docs/12_TASKS.md](../docs/12_TASKS.md).

## Current Sprint: Sprint 3 — Account Management & Note Foundation (M0 → M1)

Goal: close the remaining M0 deliverables — account management (settings + FR-AUTH-6 deletion), the command palette, and signup→shell verification — and lay the M1 *Collect* foundation: the note service backend (NOTE-01..03) and the Tiptap editor foundation (EDIT-01, the MVP's highest-risk markdown round-trip work, started early to de-risk).

Promoted 2026-07-23 by the architect role after Sprint 2 closed (M0 exit criteria already met; this sprint finishes the open M0 deliverables and opens M1). Within each area the dependency chain serializes work (GOV-5: one agent per prefix at a time); P0 marks the critical path.

**Intra-sprint sequencing (GOV-5):** NOTE-01 → NOTE-02 → NOTE-03 (backend, strict chain). AUTH-11 → AUTH-12 (deleteAccount service before its UI). AUTH-10 (frontend) and AUTH-13 (backend) are independent and can run alongside their prefix-mates once the chain head is claimed. SHELL-04 and EDIT-01 are independent L tasks (frontend). Deferred behind this sprint: NOTE-04..06, the rest of EDIT, and FOLD/TAG/ATT/DAILY (all need the NOTE/EDIT foundation); CRED-01 and the ADR-25 AI-gating ripples belong to later milestones.

| ID | Title | Priority | Cx | Depends on | Owner | Status | Milestone | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|
| AUTH-10 | Account settings page (display name) | P1 | S | AUTH-09, SHELL-03 | Claude | Done | M0 | Merged via PR #96 (`1adc6ae`) — `/settings` page under `(app)` group; server-side profile load via UserService, read-only email (JWT claims), `UpdateProfileForm` client component with `updateProfileAction` server action; ADR-23 contract enforced in service layer. Settings link added to sidebar nav above logout. 177/177 units green (9 new). Reviewer: no IDOR (session claims), no RLS bypass, email from verified JWT. **AUTH-12 and CRED-03 now have their UI anchor** |
| AUTH-11 | `UserService.deleteAccount` orchestration (FR-AUTH-6) | P1 | M | AUTH-09, DB-13 | Claude | In Review | M0 | Soft-delete all owned rows → revoke MCP credentials → Auth deletion, per [05_API §11](../docs/05_API.md#11-userservice); grace period. Unblocks AUTH-12 |
| AUTH-12 | Account deletion UI (grace-period confirmation) | P2 | M | AUTH-11 | frontend | Queued | M0 | Behind AUTH-11 |
| AUTH-13 | Signup → empty-shell provisioning E2E (FR-AUTH-5) | P2 | S | AUTH-02, SHELL-02 | backend | Queued | M0 | Cloud-gated Playwright; closes AUTH-phase verification |
| SHELL-04 | Command palette (`⌘K`) + static command registry | P1 | L | SHELL-02 | frontend | Queued | M0 | [10_DESIGN §8](../docs/10_DESIGN.md#8-keyboard-shortcuts); the last unbuilt M0 shell deliverable |
| NOTE-01 | `NoteRepository`: typed CRUD incl. envelope+subtype transactional writes | P0 | M | DB-04, DB-16 | Codex (backend) | Done | M1 | Merged via PR #87 (`7d4f44f`) — atomic envelope+subtype writes via `SECURITY INVOKER` `create_note`/`update_note` RPCs (pinned empty `search_path`; RLS is the authorization floor). Reviewer approved the SQL (incl. the `service_role`-revoke hardening) and **independently verified the live dev-Cloud catalog** — both functions INVOKER, `authenticated`-only EXECUTE (`anon`/`service_role` revoked), 18/18 migration parity, security advisors clean for the new functions. Replay tripwires assert presence/INVOKER/least-privilege. Integration proves atomic rollback (create+update), title dual-write, forged-owner `42501`, cross-user denial, soft-delete/restore; 153 units green. Next.js patched 15.5.21. **NOTE-02/NOTE-03 now unblocked** |
| NOTE-02 | `NoteService.create` (FR-NOTE-1) | P1 | M | NOTE-01, SETUP-11 | Codex (backend) | Done | M1 | Merged via PR #91 (`4f5cacf`) — exact 05_API §4 contract over the transactional NOTE-01 `NoteRepository`: non-empty string title, optional string body/folder, documented defaults (`''`/root/null date), full `Note` (`= KnowledgeObjectSummary + body/folderId/dailyNoteDate`, `type='note'`, `tags: []`) matching the §4 example. Reviewer confirmed validation precedes data access, no invented rules, and no synchronous embedding (async webhook per ADR-2). 6 units; 164 units + 31 Cloud integrations green. **Deferred to NOTE-14 (disclosed):** whitespace-only title passes (no documented trim); bad `folderId` → generic FK error (no folder-specific error defined) |
| NOTE-03 | `NoteService.get` with soft-delete visibility rules | P1 | S | NOTE-01 | Codex (backend) | Done | M1 | Merged via PR #94 (`13a910e`) — faithful ADR-26 implementation: delegates to the RLS-scoped `getNote`, returns the full public `Note` for a visible active row, and uniformly throws `NotFoundError` for `null` (nonexistent/foreign-owned/RLS-hidden) or `deletedAt !== null` (soft-deleted outside trash) — no `ForbiddenError`, no privileged existence probe. Reuses the NOTE-02 mapper; `NotFoundError` from the taxonomy. 9 units (3 new) covering all three branches; 167 units + 31 Cloud integrations green |
| EDIT-01 | Tiptap editor foundation: markdown-backed document model, live-formatting base | P0 | L | SETUP-03 | Codex (frontend) | Done | M1 | Merged via PR #89 (`bfa1e6c`) — sanctioned Tiptap 3.28 stack (react/pm/starter-kit/markdown, 10_DESIGN §5); controlled, **SSR-safe** (`immediatelyRender: false`) `MarkdownEditor` — reviewer verified the controlled-value guard (`getMarkdown() === value` + `emitUpdate: false`) prevents loops/cursor resets, accessibility (`role=textbox`/`aria-multiline`/`-label`/`-disabled`), token-only CSS, and no business logic. Parse+serialize + render/controlled/read-only tests; 158 units green. Scope correctly bounded (no route/persistence/menus). **Reviewer flag for EDIT-02/03:** FR-NOTE-2 loss-free round-trip rests on the **beta** `@tiptap/markdown` — validate against a real corpus and keep a fallback if lossy. **EDIT-02+ now unblocked** |

## Sprint 2 — Schema, Auth Core & App Shell (M0) — ✅ Complete 2026-07-22

Goal (met): the full 13-table schema live (with per-table RLS + cross-user tests, GOV-6), a working signup/login/session flow, and the three-zone app shell — everything M1's note features build on. Promoted 2026-07-17; all rows below Done. **M0 exit criteria met** (signup → login → shell in production, CI-gated); the open M0 deliverables (AUTH-10..13 account mgmt, SHELL-04 command palette) carry into Sprint 3.

| ID | Title | Priority | Cx | Depends on | Owner | Status | Milestone | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|
| DB-03 | Migration: `knowledge_objects` envelope table | P0 | M | DB-02 | database | Done | M0 | Merged via PR #10 (`318f958`) after review — SQL byte-checked against §4.2/ADR-14/GOV-6; reviewer ran the Cloud RLS suite live (green) and confirmed fail-closed behavior (see Completed). DB-04, DB-06..10 now claimable |
| DB-04 | Migration: `notes` subtype table | P0 | M | DB-03, DB-05 | database | Done | M0 | Merged via PR #17 (`c3594bf`) after review — §4.3 exact (generated tsvector, GIN, partial-unique daily-note index, ADR-14/15 FKs); reviewer ran the Cloud suite live from a worktree (8 tests green on rerun; see Completed). DB-13's gate now: DB-06..12 |
| DB-05 | Migration: `folders` | P0 | S | DB-02 | database | Done | M0 | Merged via PR #15 (`0b30c56`) after review — first ADR-15 `SET NULL` FK; reviewer ran the Cloud suite live in an isolated worktree (5 tests green incl. parent-purge → child-to-root). DB-04 now fully unblocked (see Completed) |
| DB-06 | Migration: `tags` + `knowledge_object_tags` | P0 | S | DB-03 | database | Done | M0 | Merged via PR #21 (`08d4095`) after review — §4.6–4.7 exact incl. ADR-16 `tag_id` cascade, verified live; reviewer ran the Cloud suite (10/10, no flake). Schema chain remaining: DB-07..12 |
| DB-07 | Migration: `links` | P0 | S | DB-03 | database | Done | M0 | Merged via PR #23 (`341eeb8`) after review — §4.8 exact (unique pair, both direction indexes, ADR-14 cascades); 3× consecutive 12/12 Cloud-suite passes before reviewer-induced Auth rate limiting (see Completed) |
| DB-08 | Migration: pgvector + `embeddings` | P0 | M | DB-03 | database | Done | M0 | Merged via PR #25 (`1257a09`) after review — §4.9/08_SEARCH §3 exact (1536-dim cosine HNSW); includes the harness hardening (shared user pair + bounded retry): reviewer verified 4× consecutive 14/14 suite runs, the exact scenario that previously tripped the Auth limiter (see Completed) |
| DB-09 | Migration: `attachments` + private storage bucket | P0 | M | DB-03 | database | Done | M0 | Merged via PR #27 (`6182d17`) after review — §4.4 + 09_SECURITY §4 with signed-URL-only reads enforced at DB level via operation-scoped storage policies; reviewer ran the suite live (17/17). See Completed |
| DB-10 | Migration: `chat_conversations` + `chat_messages` | P0 | S | DB-03 | database | Done | M0 | Merged via PR #29 (`bbb5042`) after review — §4.10–4.11 exact; both ADR-16/14 cascade chains verified live; reviewer ran the suite (20/20). DB-11/12 remain before DB-13 |
| DB-11 | Migration: `mcp_credentials` | P0 | S | DB-02 | database | Done | M0 | Merged via PR #31 (`d4c8d1b`) after review — §4.12 exact, hash-only storage, uniform RLS; suite 22/22 live. Only DB-12 remains before DB-13 |
| DB-12 | Migration: `audit_log` (append-only) | P0 | S | DB-02 | database | Done | M0 | Merged via PR #33 (`5d02462`) after review — ADR-16 SET NULL exception honored; append-only enforced at grant level (owner update/delete → 42501, tested live); suite 25/25. **All 13 tables live — DB-13 audit is now claimable** |
| DB-13 | RLS audit across all tables | P0 | M | DB-03..12 | database | Done | M0 | Merged via PR #35 (`70e894a`) after review — 13 tables/48 policies verified against §7, one real finding fixed (legacy profiles grants → SELECT/UPDATE-only, regression-tested); both escalated hardening candidates dispositioned in ADR-17 (declined with revisit triggers). Suite 26/26 live |
| DB-14 | Cross-user RLS integration suite (one denial test per table) | P1 | M | DB-13 | database | Done | M0 | Merged via PR #37 (`2b88641`) after review — satisfied by GOV-6's per-table tests; reviewer independently re-mapped all 13 tables to denial assertions. No duplicate suite added (correct). Only DB-15 remains in the database phase |
| DB-15 | Retention purge: `pg_cron` schedule + purge worker endpoint | P1 | M | DB-13 | database | Done | M0 | Merged via PR #40; **migration applied to Cloud 2026-07-18 via Supabase MCP** — history 17/17 matches repo, cron job `retention-purge-daily` active (03:00 UTC), Vault no-op path manually invoked, security advisors clean. Worker goes live when CI-07 stores Vault values. **Database phase complete: DB-01..16 all Done** |
| AUTH-01 | Supabase Auth config: email/password + templates | P0 | S | DB-01 | backend | Done | M0 | Merged via PR #47 (`215b198`) — ADR-19: confirmation off (FR-AUTH-1), min password 8; canonical settings in [supabase/auth-config.md](../supabase/auth-config.md) (dashboard must match it); templates staged in-repo for CI-07 (free-tier default-SMTP lock). Live-verified 2026-07-18 pre/post dashboard apply. **AUTH-02/06 (and AUTH-04 with DB-16) now claimable** |
| AUTH-02 | Signup page + form | P0 | M | SETUP-03, AUTH-01 | frontend | Done | M0 | Merged via PR #49 (`56c7a1d`) — FR-AUTH-1; `(auth)` route group + `src/features/auth/` (schema/wrapper/form); RHF + zod deps added per §2.1 stack; 17 unit tests; live smoke green. **AUTH-03 now claimable** (should add the login cross-link on `/signup`) |
| AUTH-03 | Login page + form | P0 | M | AUTH-02 | frontend | Done | M0 | Merged via PR #51 (`a3dadc6`) — FR-AUTH-1; `/login` with neutral credential-error mapping (no account-existence leak); signup↔login cross-links; shared `AuthFormField`; 16 unit tests; live smoke green. AUTH-14 half-unblocked (needs AUTH-06) |
| AUTH-04 | Session cookie handling + middleware refresh | P0 | M | AUTH-01, DB-16 | backend | Done | M0 | Merged via PR #53 (`e77fa1c`) — ADR-20: server-side-only auth; HttpOnly + SameSite=Lax proven in a live browser e2e (incl. `document.cookie` exposure check); middleware `getClaims()` refresh; sign-up/in are server actions; browser client deleted; OBS-02 resolver wired. **AUTH-05/07/08 now claimable** |
| AUTH-05 | Auth middleware: protect `(app)` routes | P0 | S | AUTH-04 | backend | Done | M0 | Merged via PR #55 (`b9fd739`) after review — fail-closed page gate over ADR-20 refresh; cookie rotation survives redirects; reviewer re-ran units (81/81) + live Playwright (2/2) in an isolated worktree. Allowlist must grow with AUTH-06/07 (recorded in handoff). **All P0 auth tasks Done** |
| AUTH-06 | Password reset flow | P1 | M | AUTH-01 | Claude | Done | M0 | Merged via PR #70 (`602710c`) — FR-AUTH-3; server-side request→callback→completion (ADR-20). `/forgot-password` + `/reset-password` pages, `/auth/recovery/callback` handles PKCE `code` (dev default template) and `token_hash` recovery (staged prod template); middleware allowlists the public request/callback routes and keeps `/reset-password` gated. The callback binds session cookies to its redirect response (same chokepoint as AUTH-04 middleware). Shared 8–72 password policy extracted. 123 units + **live recovery e2e green against the dev project** (admin-generated recovery token → callback → HttpOnly session → updateUser → re-login with new password). Merge required an unrelated fix first — SEC-07 (#71) pinned `sharp ^0.35.3` to clear a high-sev libvips advisory that was failing the audit gate repo-wide. **AUTH-14 now dependency-ready** |
| AUTH-07 | Google OAuth provider | P1 | M | AUTH-04 | backend | Done | M0 | Merged via PR #79 (`0bb18bb`) — FR-AUTH-2; server-side Google PKCE (ADR-20 preserved): fixed provider + `/auth/oauth/callback`, `redirectTo` from validated same-origin only, no browser Supabase client; verifier/session in HttpOnly cookies (proven not JS-readable). Reviewer verified the extraction refactor is behavior-preserving — origin validation → `request-origin.ts` and callback cookie-plumbing → `auth-callback-session.ts`, byte-identical to AUTH-06 and deferred to the 2nd consumer; recovery/reset paths delegate with no behavior change. Fixed redirects only; generic `?error=oauth` alert on login; middleware makes only the callback public. 145 units + 29 Cloud integrations green. **Manual QA closed 2026-07-22 (user):** real Google consent round-trip verified working end-to-end — provider client ID/secret wiring confirmed. AUTH-07 fully verified |
| AUTH-08 | Logout (server-side refresh-token revocation) | P1 | S | AUTH-04 | backend | Done | M0 | Merged via PR #66 (`40b3b81`) — ADR-22: `signOut({ scope: "local" })` revokes only the current refresh token (not SDK-global default), clears ADR-20 HttpOnly cookies, redirects `/login`. 2 units; suite 86/86; temporary live e2e proved signup → logout → no `sb-*` cookies → `/login` and was removed. **SHELL-03 must bind the visible trigger** |
| AUTH-09 | `UserService.getProfile` / `updateProfile` | P1 | S | DB-02, SETUP-11 | backend | Done | M0 | Merged via PR #76 (`5c213c0`) — ADR-23 contract implemented by Codex; reviewer verified: exact `Profile` shape, email from verified JWT claims (never input/table), fail-closed `claims.sub === userId` check, all `profiles` access confined to `UserRepository` under existing RLS, no service-role client. `updateProfile` trim/null-clear/≤80-Unicode-code-point validation runs before data access; omitted field is a read-only no-op. Necessary+safe `errors.ts` fix (base ctor `public`, class stays `abstract`). 135 units + 29 Cloud integrations green. Establishes `src/features/user/`. **AUTH-10, AUTH-11, CRED-01 now dependency-ready** |
| AUTH-14 | Auth error states | P2 | S | AUTH-03, AUTH-06 | frontend | Done | M0 | Merged via PR #77 (`dea2d31`) — audit-only completion; reviewer independently traced each acceptance criterion to shipped, tested code: wrong-password `invalid_credentials` → neutral alert with no navigation (AUTH-03); existing-email `user_already_exists` → specific alert with no navigation (AUTH-02); expired/rejected recovery credentials → fixed `/forgot-password?error=invalid-link` redirect + announced `role="alert"` guidance, plus completion-time `session_not_found`/`bad_jwt` → `invalid-session` (AUTH-06). Covered at action/route/page/component boundaries (38 focused tests). No executable change added — new code would be dead/duplicate |
| SHELL-01 | Root layout: fonts, theme provider, toaster | P0 | S | SETUP-02, SETUP-13 | frontend | Done | M0 | Merged via PR #13 (`f016273`) after review — fonts wired to token vars, theme-bound sonner Toaster, resolver de-duplicated; reviewer fixup for stray conflict markers (see Completed). SHELL-02 now claimable |
| SHELL-02 | Three-zone app shell | P0 | L | SHELL-01 | frontend | Done | M0 | Merged via PR #64 (`282e594`) after review — 10_DESIGN §3.2 exact; server-composed with a single client island per rail; L-split recorded (SHELL-03 nav, SHELL-05 shortcuts, SHELL-06 responsive, SHELL-10 motion); reviewer re-ran 84/84 units + live Playwright 2/2 in an isolated worktree. **SHELL-03 now claimable** |
| SHELL-03 | Sidebar navigation frame | P1 | M | SHELL-02 | frontend | Done | M0 | Merged via PR #68 (`17b116a`) — semantic Daily note/Folders/Tags section frames without invented routes; server-composed AUTH-08 logout trigger; collapsed rail removes child content. 3 units; suite 89/89; build + six required checks green. E2E logout extension committed but local shared-Cloud run denied by auto policy. **AUTH-10 and later sidebar feature tasks unblocked** |
| SHELL-07 | TanStack Query provider + defaults | P1 | S | SETUP-01 | frontend | Done | M0 | Merged via PR #14 (`13b3ff6`) after two-round review — 60s staleTime/retry-1/no-refocus defaults, devtools dev-gated in devDependencies, nested inside ThemeProvider (reported with rationale). The oldest open PR is cleared |
| SHELL-10 | Motion tokens + `prefers-reduced-motion` | P2 | S | SETUP-02 | frontend | Done | M0 | Merged via PR #73 (`d6d882c`) after review — 10_DESIGN §9 exact: 150ms micro + 250ms structural tokens (two durations only, no raw ms/arbitrary values), both 0ms under `prefers-reduced-motion`; shell panels get `transition-width`+`overflow-hidden` structural motion, shared buttons keep their hover micro transition; ease-out entrances / ease-in exits. Reviewer (Claude, independent — Codex authored) checked out the branch, re-ran 125 units + typecheck/lint/format/build, and confirmed the utilities + reduced-motion collapse compile into the built CSS. No dependencies added |
| CI-04 | Supabase migration check in CI | P0 | M | DB-01, CI-01 | backend | Done | M0 | Merged via PR #62 (`e8b3ef0`) after review — ADR-13/21 exact; reviewer independently re-verified fixture provenance (upstream blob at pinned commit hashes identical) and live protection (strict, six Actions-pinned contexts). Post-replay assertions close the DB-09 storage-helper note. **13-table assertion is a deliberate tripwire: schema migrations must update it.** Sprint 2 CI track: only CI-07 remains |
| CI-05 | `npm audit` gate (high severity) | P1 | S | CI-01 | backend | Done | M0 | Merged via PRs #57 (`506a48e`, workflow) + #58 (`574ad7a`, record) after review — `Dependency audit` job green in 12s on its own PR; reviewer read back live protection: strict, five contexts, all app-pinned to GitHub Actions. Registry availability is an accepted external dependency; SEC-07 owns pinning review |
| CI-07 | Per-environment env vars (preview vs. production) | P1 | M | CI-02 | Codex (backend) | Done | M0 | Merged via PR #82 (`55f917e`) — isolated production Supabase project (`hqzakxpbxqzxismmgnyn`) + 17-migration parity, environment-scoped Supabase/webhook values, custom SMTP/templates, Google OAuth, canonical `brain.khaire.dev` Auth + retention-purge wiring, Preview Protection. Reviewer verified **no committed secrets** (project refs are public), the `12_TASKS`/`auth-config` scope+config edits, and ADR-24. **User confirmed production `brain.khaire.dev` serving + Google login working end-to-end.** **ADR-24 (explicit user decision):** distinct Preview/Production `OPENAI_API_KEY` moves to EMB-01, before first live OpenAI use. GOV-7/ADR-12 preserved. Residual: SMTP delivery to a real inbox not automated. **Sprint 2 queue complete** |
| OBS-02 | Request logging across Web API/MCP/webhook boundaries | P1 | M | OBS-01 | backend | Done | M0 | Merged via PR #44 — `withRequestLogging` on both existing routes; route identity in event names (content-free), AUTH-04 resolver slot, MCP-01/EMB-02 adopt the wrapper when built. 31 unit tests green |

Promoted to Sprint 3 (2026-07-23): AUTH-10..13, SHELL-04, plus the M1 foundation NOTE-01..03 and EDIT-01. Still deferred (naturally later): SHELL-05/06/08/09 (shell polish), CI-06 (E2E against previews) → CI-08 (axe a11y, needs CI-06) — candidates for Sprint 4 alongside the rest of NOTE/EDIT.

## Sprint 1 — Repo & Tooling Foundation (M0) — ✅ Complete 2026-07-17

All 21 tasks Done (SETUP-01..14, DB-01, DB-02, DB-16, OBS-01, CI-01..03) — see Completed below for per-task review records.

## Completed

- **SETUP-01..14** — implemented by Codex (`22c74bc`), reviewed and verified by Claude (reviewer role) on 2026-07-16: typecheck/lint/format/unit tests green, custom lint rules probe-verified, production build clean, token values match ADR-8, theme plumbing matches ADR-9. Playwright E2E not independently re-run by the reviewer.
- **DB-01** — implemented by Codex (`ade209c`), reviewed by Claude (reviewer role) on 2026-07-16: ADR-10 spec edits internally consistent, no secrets committed, no migration files (infrastructure-only, as scoped). Three ripple gaps fixed in review (03 §8 env table, CI-04 wording, `agents/database.md`). Cloud-project state (`zkzyfwclvquiargnwgtw`) not independently verifiable from the repo; CI-04 mechanism decision opened in PROJECT_STATE.
- **DB-02** — implemented by Codex (`1c5e2e3`), reviewed by Claude (reviewer role) on 2026-07-16: SQL matches ADR-11 exactly (RLS shape, `SECURITY DEFINER` + pinned `search_path`, initplan-cached `auth.uid()`, no INSERT/DELETE policies); FK cascade recorded back into 04 §4.1. **Conditional pass:** the cross-user denial check was manual-against-Cloud, not a committed test — owed by DB-16's harness; queue-AC rewording by the implementer flagged as a GOV-2 process violation. Cloud state and advisor results implementer-attested. **Condition cleared 2026-07-16:** DB-16 delivered the repeatable test; reviewer ran it green against Cloud.
- **CI-01** — implemented by Codex (PR #1, merged `38c1282`), reviewed and merged by Claude (reviewer role) on 2026-07-17: four independent jobs with stable context names for CI-03, Node 22.12 pinned, least-privilege permissions, per-PR concurrency cancellation; all checks green on GitHub runners. First task through the full branch → PR → review → merge pipeline. Non-blocking findings: no `push: main` trigger (mitigate via CI-03's up-to-date-branches requirement); actions tag-pinned rather than SHA-pinned (optional hardening, SEC-07 territory).
- **CI-02** — implemented by Codex (PR #2, merged `2a634a0`), reviewed and merged by Claude (reviewer role) on 2026-07-17: `vercel.json` pins `framework: nextjs` (fixing Vercel's `Other` misclassification), `.vercel` ignored, no secrets or new infra introduced. Reviewer probed the PR preview URL live (HTTP 200) and verified the **first production deployment succeeded on the merge commit** — the one path the PR itself couldn't exercise. Env vars intentionally deferred to CI-07. Non-blocking note: preview deployments are publicly reachable; revisit Vercel Deployment Protection when CI-07 attaches the shared dev Supabase env to previews.
- **CI-03** — implemented by Codex (PR #3, merged `64e0c2c`), reviewed and merged by Claude (reviewer role) on 2026-07-17: live protection read back from the GitHub API — `strict: true` (up-to-date branches, per the CI-01 review finding), exactly the four required contexts pinned to the GitHub Actions app, `enforce_admins: true`, force pushes and deletions disabled. Reviewer proved the block empirically: a direct push to `main` was rejected, and this review record itself merged through the gated PR path. Repository made public by explicit user decision to enable protection — recorded as GOV-7 with a clean full-history secret scan and the standing fork-PR constraint (credentialed CI jobs must never be fork-triggerable). No review-count requirement was added (correctly, not in the AC). Sprint 1 P0s complete.
- **OBS-01** — implemented by Codex (PR #5, merged `f367cc1`), reviewed and merged by Claude (reviewer role) on 2026-07-17: content-free logging enforced structurally — metadata values admit only boolean/number/null (strings unrepresentable at type and runtime level), events must match a stable-identifier pattern, identifiers validated against injection (newline-forgery rejected), `toJSON` serialization-hook smuggling defeated by plain-copy serialization, context snapshot immune to caller mutation. Zero dependencies (JSON lines to console per 03 §9's open mechanism — no infra added). Reviewer re-ran the 11-test suite locally: green. Unauthenticated requests log `userId: null` rather than an invented identity. Known limit (honestly reported): the module boundary can be bypassed with direct `console` calls — SEC-06 owns that audit; OBS-02 wires request boundaries. Sprint 1 complete.
- **DB-03** — implemented by Codex (PR #10, merged `318f958`), reviewed and merged by Claude (reviewer role) on 2026-07-17: migration SQL matches [04_DATABASE §4.2](../docs/04_DATABASE.md#42-knowledge_objects) exactly — seven columns with documented defaults, `note|attachment` CHECK, `owner_id → profiles.id ON DELETE CASCADE` (ADR-14), both composite indexes; RLS in the uniform §7 shape (four policies, initplan-cached `(select auth.uid())`, `authenticated` only). Second forward-only migration strips legacy default privileges (`REFERENCES`/`TRIGGER`/`TRUNCATE`) and re-grants CRUD — least-privilege improvement found by the implementer via live inspection, honestly reported. GOV-6 satisfied: cross-user denial test in the same PR covering read/update/delete (empty-result semantics), insert-as-other-owner (42501), CHECK rejection (23514), owner self-access. **Reviewer ran the full Cloud integration suite live: green (2 files, 3 tests)** and reconfirmed the harness fails closed without credentials. Repo↔Cloud migration-history match remains implementer-attested until CI-04 automates it. Transient Cloud `JWT issued at future` flake honestly disclosed (single rerun passed) — watch for recurrence.
- **DB-05** — implemented by Codex (PR #15, merged `0b30c56`), reviewed and merged by Claude (reviewer role) on 2026-07-17: SQL matches [04_DATABASE §4.5](../docs/04_DATABASE.md#45-folders) exactly — `owner_id` cascade (ADR-14), **first `parent_folder_id ON DELETE SET NULL` (ADR-15)**, composite `(owner_id, parent_folder_id)` index, uniform RLS; the DB-03 review lesson was folded in unprompted (legacy default privileges revoked in the same migration). Test covers cross-user denial AND the ADR-15 behavior live (physically deleted parent → child `parent_folder_id` becomes null). **Reviewer ran the full Cloud suite live from an isolated worktree: green (3 files, 5 tests).** Non-blocking observation for DB-13's audit scope: FK constraints bypass RLS, so a direct API caller who somehow knew another user's folder UUID could reference it as `parent_folder_id` — harmless in practice (UUIDs unguessable, service layer validates parents under RLS), but the audit should consider composite same-owner FKs as hardening.
- **SHELL-01** — implemented by Antigravity (PR #13, merged `f016273`), reviewed and merged by Claude (reviewer role) on 2026-07-17 — **first Antigravity task through the pipeline.** Substance: Inter + JetBrains Mono via `next/font` wired to the exact CSS variables the SETUP-02 token scaffold consumes; client ThemeProvider coordinating with the existing `/api/theme` cookie route (ADR-9); sonner Toaster rendered inside the provider with `theme={theme}` so toasts follow light/dark/system live. First review round: stale-base DIRTY state had suppressed all required checks; both non-blocking items (theme-bound toaster, resolver extraction into `theme.ts`) were then fixed well. Reviewer fixup `39639a0` before merge: two orphaned conflict markers in AI_HANDOFF.md and a Files-Added line referencing files not in the PR. Process note: prettier's Format check does not catch conflict markers in markdown — CI blind spot worth a cheap guard later.
- **DB-04** — implemented by Codex (PR #17, merged `c3594bf`), reviewed and merged by Claude (reviewer role) on 2026-07-17: SQL matches [04_DATABASE §4.3](../docs/04_DATABASE.md#43-notes) exactly — subtype PK FK → `knowledge_objects.id` cascade (ADR-14), `owner_id` cascade, `folder_id ON DELETE SET NULL` (ADR-15), `body` default `''`, unweighted `to_tsvector('english', title || ' ' || body)` generated column exactly as [08_SEARCH §2](../docs/08_SEARCH.md#2-full-text-search) specifies (unweighted by design; `ts_rank_cd` ranks at query time), GIN index, partial unique `(owner_id, daily_note_date)` index, uniform RLS + least-privilege grants in one migration. Tests cover cross-user denial, FTS matching via the generated vector, daily-note uniqueness (`23505`), and ADR-15 folder-purge → note-to-root. **Reviewer ran the Cloud suite live from an isolated worktree: 8/8 green on rerun.** One transient first-run failure = the known Cloud JWT clock-skew flake — **second occurrence; harness retry hardening now warranted** (logged in Known Technical Debt).
- **DB-06** — implemented by Codex (PR #21, merged `08d4095`), reviewed and merged by Claude (reviewer role) on 2026-07-17: both tables match [04_DATABASE §4.6–4.7](../docs/04_DATABASE.md#46-tags) exactly — case-insensitive per-owner uniqueness as an expression index, composite PK + `(tag_id, owner_id)` secondary, all three ADR-16/14 cascades in place, uniform RLS + least-privilege grants. Tests cover duplicate-name rejection (`23505`), cross-user denial on both tables, cross-owner association insert (`42501`), and **live ADR-16 verification: deleting a tag removes its join rows**. Reviewer ran the full Cloud suite in the shared checkout (single-agent workflow now): 10/10 green, no clock-skew flake. Note: the harness retry ride-along from Known Technical Debt was not included — still open.
- **DB-07** — implemented by Codex (PR #23, merged `341eeb8`), reviewed and merged by Claude (reviewer role) on 2026-07-17: SQL matches [04_DATABASE §4.8](../docs/04_DATABASE.md#48-links) exactly — five columns, `UNIQUE (source_object_id, target_object_id)` pair constraint, both direction indexes (`target_object_id` = the FR-LINK-5/6 backlinks path), ADR-14 cascades on all three FKs, uniform RLS + least-privilege grants. Tests: duplicate edge → `23505`, cross-user denial, cross-owner insert → `42501`, and endpoint-deletion cascade verified live (deleting source or target removes the edge). **Reviewer ran the Cloud suite repeatedly: three consecutive 12/12 full passes on the merged HEAD.** Review discovery: repeated suite runs tripped Supabase Auth's rate limiter (`Unable to authenticate an isolated Cloud integration-test user`; suites fail-closed → tests skip) — an environmental failure mode, not a schema defect; Known Technical Debt updated (harness needs backoff/retry + user reuse as the suite grows toward 13 files × 2 users/run).
- **DB-08** — implemented by Codex (PR #25, merged `1257a09`), reviewed and merged by Claude (reviewer role) on 2026-07-17: migration matches [04_DATABASE §4.9](../docs/04_DATABASE.md#49-embeddings) + [08_SEARCH §3](../docs/08_SEARCH.md#3-semantic-search-pgvector) exactly — pgvector in `extensions`, `vector(1536)` (07_AI small-tier), `UNIQUE (knowledge_object_id, chunk_index)`, cosine-ops HNSW, ADR-14 cascades, uniform RLS + least-privilege grants. **Also delivers the harness hardening ride-along:** one shared user pair provisioned in vitest global setup (2 Auth sign-ins/run instead of 12, service-role now confined to setup — tighter ADR-12 containment), bounded 1s/2s/4s retry on 429/clock-skew only, fail-closed and dev-project hostname pin preserved (reviewer-verified in source). **Reviewer ran 4 consecutive full-suite passes (14/14) including back-to-back repetition — the exact scenario that tripped the Auth limiter in the DB-07 review.** Both Known Technical Debt harness items closed. Implementer honestly flagged: `embeddings.owner_id` FK has no owner-leading index (documented index set doesn't include one; RLS predicates filter by owner) — routed to DB-13 to assess owner-leading indexes consistently across all tables.
- **DB-09** — implemented by Codex (PR #27, merged `6182d17`), reviewed and merged by Claude (reviewer role) on 2026-07-17: subtype table exact to [04_DATABASE §4.4](../docs/04_DATABASE.md#44-attachments) (ADR-14 cascades, uniform RLS, least-privilege); private `attachments` bucket with owner-scoped path policies (`storage.foldername(name)[1] = auth.uid()`, the documented defense-in-depth from [09_SECURITY §4](../docs/09_SECURITY.md#4-attachment-security)); and — the notable part — **"reads exclusively via signed URLs" enforced at the DB, not by convention**: the storage SELECT policy is operation-scoped to sign/delete/delete-many only, arrived at through three honest forward-only migrations (initial → sign-only → sign+delete-lookup after the middle state broke deletes and leaked two objects, which the implementer's own cleanup audit caught, removed, and regression-tested). Tests include a real Storage round-trip, cross-user denial on every operation, owner-prefix enforcement, and **owner direct-download denial** — the assertion that proves the signed-URL-only property. **Reviewer ran the full Cloud suite live: 8 files, 17/17 green.** No undocumented size limit/MIME allowlist added (service-layer per spec, correctly deferred). CI-04 ripple noted on its queue row: the replay image must carry the operation-scoped storage helpers.
- **DB-10** — implemented by Codex (PR #29, merged `bbb5042`), reviewed and merged by Claude (reviewer role) on 2026-07-17: both tables exact to [04_DATABASE §4.10–4.11](../docs/04_DATABASE.md#410-chat_conversations) — scope/role CHECKs, `citations` jsonb, `(conversation_id, created_at)` retrieval index, ADR-16 `conversation_id` cascade, ADR-14 `note_id` cascade, uniform RLS + least-privilege on both tables; scope↔note_id consistency correctly left to the service layer per spec. Tests: CHECK rejections (`23514`) for scope and role, cross-user denial on both tables, and **both cascade chains live** — conversation→messages, and the two-level note-object→conversation→messages. **Reviewer ran the full Cloud suite: 9 files, 20/20 green.**
- **DB-11** — implemented by Codex (PR #31, merged `d4c8d1b`), reviewed and merged by Claude (reviewer role) on 2026-07-17: exact to [04_DATABASE §4.12](../docs/04_DATABASE.md#412-mcp_credentials) — all seven columns, `token_hash` only (raw token never stored), nullable `last_used_at`/`revoked_at`, ADR-14 owner cascade, uniform RLS + least-privilege. No undocumented `token_hash` lookup index invented — that decision belongs to the CRED tasks that build MCP auth. **Reviewer ran the full Cloud suite: 10 files, 22/22 green.**
- **DB-12** — implemented by Codex (PR #33, merged `5d02462`), reviewed and merged by Claude (reviewer role) on 2026-07-18: exact to [04_DATABASE §4.13](../docs/04_DATABASE.md#413-audit_log) — actor CHECK, structured-metadata jsonb, chronological index, and the two properties that made this table special, both implemented and **proven live**: (1) **ADR-16's SET NULL exception** — object purged, audit row retained with nulled reference (the do-not-cascade warning on the queue row was heeded); (2) **append-only at the grant level** — only SELECT/INSERT granted, no update/delete policies exist, and the tests show even the *owner* gets `42501` on update/delete; account-deletion erasure still works because FK cascades execute with table-owner privileges. **Reviewer ran the full Cloud suite: 11 files, 25/25 green. All 13 schema tables are now live** — the M0 schema chain (DB-02..DB-12) is complete; DB-13's audit is claimable with its three accumulated notes.
- **DB-13** — implemented by Codex (PR #35, merged `70e894a`), reviewed and merged by Claude (reviewer role) on 2026-07-18: full-catalog audit of all 13 tables against [04_DATABASE §4/§7](../docs/04_DATABASE.md#7-row-level-security-rls-policies) — 48 policies verified in the documented shape, all documented indexes present, FK delete actions per ADR-14/15/16, storage policies intact, advisors clean. **One real finding, fixed in-PR:** `profiles` retained legacy broad grants from DB-02 (predating the least-privilege pattern DB-03 introduced) — RLS already blocked the writes, so defense-in-depth gap rather than exposure; corrected to SELECT/UPDATE-only with a regression test (owner INSERT/DELETE → `42501`). **Both escalated hardening candidates dispositioned as ADR-17** (composite same-owner FKs and blanket owner/FK indexes: declined for MVP with recorded revisit triggers) — the audit correctly escalated rather than deciding. **Reviewer ran the suite live: 26/26 on rerun**; residual-flake observation logged (harness retry covers auth, not the first post-sign-in data request). Schema phase closes with DB-14/15.
- **DB-14** — completed by Codex (PR #37, merged `2b88641`), reviewed and merged by Claude (reviewer role) on 2026-07-18: a coverage-audit completion, not new code — GOV-6's same-PR rule already forced every table's cross-user denial test, so DB-14 verifies the accumulated coverage (13 tables → 11 suite files, each with user-B read denial + contract-appropriate write denial). **Reviewer independently re-mapped all 13 tables to their denial assertions — mapping confirmed.** Zero executable files changed; suite state identical to the 26/26 run from the DB-13 review. Completion logic mirrors GOV-6's own redefinition of DB-13 (per-table shipping made the batch task an audit). Honest scope: no duplicate suite padded on to look like work.
- **DB-16** — implemented by Codex (`f4be50b`), reviewed and verified by Claude (reviewer role) on 2026-07-16: typecheck/lint/format/unit tests/build green; **Cloud integration test run live by the reviewer and passed** (two isolated users, cross-user read/update denied, self-access preserved, cleanup verified) under Node 23; harness pins the dev-project hostname and fails closed; `src/` provably does not import test code; env-inlining bug fixed with static references; anon→publishable-key rename ripple complete. Two implementation decisions (publishable-key naming, Node ≥ 22.12 engine floor) were made without DECISIONS entries — accepted post-hoc in review, noted as a recurring reporting gap.
